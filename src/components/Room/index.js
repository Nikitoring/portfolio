import './room.scss'

import React, {useEffect} from 'react'
import { useSelector } from 'react-redux'
import { getInLimit } from '../../utils/index.js'

export default function RoomComponent(props) {

  return (
      <section>
      <div>
      </div>
      <div>
        ALARM@@@@@
      </div>

      </section>
  )
}